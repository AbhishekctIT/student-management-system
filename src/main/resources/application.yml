spring:
  application:
    name: student-management-system

  profiles:
    active: dev

  datasource:
    url: jdbc:mysql://localhost:3306/student_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

server:
  port: 8080

# ? CORRECT JWT Configuration
jwt:
  secret: n0ccgR5dRVdiZ3VFsSIriRak/C1R6tVxZhOrfrcHg81XvpHR6b+Y3g3o216dlTwrT96NN5PSM6g0/q6sJYyE9g==
  expiration: 86400000

logging:
  level:
    com.platformcommons: DEBUG
    org.springframework.security: DEBUG